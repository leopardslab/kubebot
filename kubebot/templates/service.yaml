apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubebot.fullname" . }}
  labels:
    {{- include "kubebot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubebot.selectorLabels" . | nindent 4 }}
